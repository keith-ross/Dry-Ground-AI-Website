
# API Configuration
PORT=3001
NODE_ENV=development

# Email Service
# The SENDGRID_API_KEY should start with "SG." and be followed by a long string
# This key is loaded into environment variables, but the actual key should be stored in Replit Secrets
SENDGRID_API_KEY=${SENDGRID_API_KEY}

# Database Configuration
# Using SQLite for local development
DB_PATH=./data/database.sqlite
# Application environment variables
NODE_ENV=development
PORT=3001

# SendGrid configuration 
SENDGRID_API_KEY=
FROM_EMAIL=noreply@example.com
ADMIN_EMAIL=admin@example.com

# For production, set this to your deployed URL
# APP_URL=https://your-app.replit.app
