# API Configuration
PORT=3001
NODE_ENV=development

# Email Service Configuration
# The SENDGRID_API_KEY should be loaded from Replit Secrets
SENDGRID_API_KEY=
FROM_EMAIL=noreply@example.com
ADMIN_EMAIL=admin@example.com

# For local development, we'll use port 3001 for the API
API_URL=http://localhost:3001

# Database Configuration
# Using SQLite for local development
DB_PATH=./data/database.sqlite

# Application environment variables
#NODE_ENV=development  (already defined above)
#PORT=3001 (already defined above)


# For production, set this to your deployed URL
# APP_URL=https://your-app.replit.app